# SDD ledger for plan: docs/superpowers/plans/2026-08-02-brain-multi-workspace-sync-rollout.md

Base: f28598469e58fc942de9a80a6fe3e292c4ea31e2
Starting version: 0.31.10
Branch: feat/multi-workspace-sync-rollout
Worktree: /Users/juanpablosarmiento/src/worktrees/brain/feat/multi-workspace-sync-rollout

Preflight: complete. See `preflight-audit.md`.

Preflight ruling: current code is authoritative. Preserve `WorkspacePaths` as the sole UUID-derived runtime path authority; do not add a redundant `SyncRuntime`. Manifest schema 1 remains current. Remote manifestless nonempty adoption is one shared Task 2/4 contract and fails closed until Task 4 adds explicit adoption. Receiver freshness remains owned by the exact live TUI. Preserve the documented markdown-to-PDF prerequisite during Task 6.

Execution ruling: the user's explicit workflow overrides SDD's per-task independent-review step and workspace-deletion finish step. Each implementer must self-review and pass task-local gates; no independent reviewer runs between tasks. One highest-capability independent reviewer runs only after all implementation and full gates. Preserve the branch and worktree.

Baseline gate: `cargo test --release` passed at f285984 (1271 unit tests plus all integration/doc tests; no failures).

Task 1: RED `cargo test --release --test sync_local local_rclone_populates_the_uuid_scoped_production_workdir -- --exact --nocapture` failed exit 101 because the harness bypassed the UUID-derived production workdir.
Task 1: minor (resolved in Task 7): pre-existing macOS receiver test cleanup could return ENOTCONN when the peer closed before `shutdown(Both)`. A final Task 7 release run reproduced it; cleanup now accepts only the already-disconnected terminal state, with no sleep or production change. The exact test passed 20/20, the receiver integration passed 21/21, and the full release suite passed.
Task 1: complete (commits f285984..64a70f8, self-review and task-local gates clean; version 0.31.11).

Task 2: RED `cargo test --release --test sync_workspace_identity` failed exit 101 on missing `brain::sync::identity`; compiled entry-point RED then proved sync/repair/check accepted a mismatched remote before the gate.
Task 2: complete (commits 64a70f8..f617e15, self-review and task-local gates clean; version 0.31.12). Shared `VerifiedRemote` gate now precedes all remote data lanes; setup initializes only a successfully listed empty target and verifies exact manifest bytes by read-back before credential persistence/baseline.

Task 3: RED compiled child bootstrap accepted a mismatched expected UUID, and subsequent compile REDs proved detached runner/lock/freshness/watcher injection seams were absent.
Task 3: complete (commits f617e15..e25bb45, self-review and task-local gates clean; version 0.31.13). Detached children carry canonical selector plus `BRAIN_WORKSPACE_ID`; bootstrap fails closed; concurrency, receiver freshness, and watcher lifecycle use injected clocks/runners and bounded polling with no fixed sleeps.

Task 4: RED setup CLI lacked `adopt_workspace_id`; subsequent REDs proved no exact UUID authorization, identity summary, interactive decision, or unreadable-manifest refusal existed.
Task 4: complete (commits e25bb45..dfa17eb, self-review and task-local gates clean; version 0.32.0). Nonempty manifestless adoption requires `--adopt-workspace-id <exact-selected-uuid>` or explicit interactive `y/yes`; `--yes` is rejected; exact manifest publication/read-back precedes every other write; schema 1 compatibility remains authoritative.

Task 5: RED coordinator module was absent; inactive task migration downgraded schema 3 to 2; journal/backup/CLI/recovery seams were absent; strengthened compiled preflight RED exposed access-mode seeding before mapping refusal.
Task 5: complete (commits dfa17eb..67e3ac2, self-review and task-local gates clean; version 0.33.0). `brain workspace migrate` is UUID-scoped, compatibility/remote/mapping/ack gated, final-legacy-sync-first, backed up, atomic, resumable, idempotent, fully verified, and ordinary startup/sync cannot activate it. Strict Clippy and full release suite green.

Task 6: RED began with missing centralized requirement types, then exposed partial sync being collapsed to off, disabled-receiver leakage, malformed typed receiver fields being treated as off, status/list/doctor filesystem writes, SQLite WAL mutation from ordinary read-only opens, missing Codex doctor parity, bundled core skills being misclassified as optional, and a read-only detached child bypassing expected-workspace UUID validation.
Task 6: complete (commits 67e3ac2..f4f69a8, self-review and task-local gates clean; version 0.34.0). Required availability is distinct from optional off/ready/incomplete health; every inspector reloads and UUID-checks only the selected workspace; status/list/doctor are literal no-write and redacted; Claude/Codex doctor health is equivalent; startup still owns the PDF prerequisite. Strict Clippy and full release suite green.

Task 7: RED composed acceptance first resolved the authenticated family sender as `family-member` instead of portable `wife`; the next run reached the real task script and exposed missing temporary `tasks/` provisioning. The two-workspace local-rclone RED then showed the old harness helper forced one fixed workspace ID, so the family workdir did not exist.
Task 7: final release RED reproduced Task 1's deferred macOS receiver cleanup race when a peer close made test-side `shutdown(Both)` return `NotConnected`; cleanup now accepts only that already-disconnected state, exact test 20/20 and receiver suite 21/21 green, with no sleep or production change.
Task 7: complete (single local task commit after f4f69a8, self-review and task-local gates clean; version 0.34.1). One hermetic scenario proves all eleven personal-plus-family lifecycle assertions through real Brain seams with fake external transports. The gated local-rclone complement proves concurrent UUID-scoped workdirs/baselines and pre-bisync mismatch refusal; watcher lifecycle uses bounded channels. Strict Clippy and full release suite green. Deferred minor: repo-wide rustfmt drift inherited from earlier branch tasks remains for Task 8, while every Task 7 Rust file passes focused rustfmt check.

Task 8: RED found the pre-release version, legacy docs-security assertions, stale `Alt-?` root help, and stale single-root/Claude Cargo description. Focused assertions failed before each corresponding release-surface change and then passed.
Task 8: complete in the local task commit after af1c502 (final version 0.35.0). All required durable docs, AGENTS contracts, CLI help, Cargo metadata, and release surfaces now match the Phase 5 module tree and explicit migration/remote identity/requirements/acceptance behavior. Full release, strict Clippy, personal-data, Python skill, local-rclone, watch, migration, acceptance, privacy, read-only, docs, help, temporary CLI smoke, stale-language, and patch-hygiene gates are green.
Task 8: deferred minor: exact base f285984 already has 1,310 repo-wide rustfmt diff lines/100 headers. Focused edition-2024 `skip_children=true` formatting over 102 Phase 5/Task 8 files passes; final repo-wide audit has 1,003 lines/76 headers and zero current-only path-normalized drift versus the exact base. No unrelated formatting sweep was retained.

Final review fix pass: RED assignment preflight selected the legacy `assignee` value when it preceded a divergent canonical `assigned_to` column. RED identity ordering showed compiled sync created its UUID bisync workdir before refusing a mismatched remote. RED argv coverage showed `tasks/SCHEMA.json` was still in the generic lane. RED setup coverage showed the identity/credential/baseline stages had no shared UUID-lock boundary. RED concurrent-empty-setup coverage observed two canonical manifest publications. RED migration planning omitted a remote schema-transition step, and transition tests initially had no publication API. Two compiled final-sync regressions then proved stale in-memory config: a pulled sender mapping was not refused, and pulled `enable_triage_habits=false` still left managed triage rows. The real-rclone two-machine regression reached a byte-convergence failure because independently migrated headers placed `task_id` differently. All REDs were observed before their respective production changes.

Final review fix pass: GREEN now prefers `assigned_to`, delays bisync workdir creation and stale-lock reaping until identity succeeds, excludes task schema metadata, and holds setup's UUID lock through the full baseline. Empty-remote setup uses append-only exact-manifest claims, deterministic UUID election, canonical re-probe, immutable-copy defense, and exact read-back. Migration reloads config/users/assignments immediately after its journaled final legacy sync, holds the UUID lock across local migration plus remote transition, publishes current CSVs then durable baselines then schema metadata, and canonicalizes migrated headers as `task_uuid,task_id`. A real-rclone legacy → migrate → immediate-sync → second-legacy-machine-join sequence converges.

Final review crash audit: RED compiled ordinary sync reached rclone while the rollout journal was active, and RED setup entered remote identity under the same condition. Both are GREEN behind one active-rollout guard inside the UUID-lock boundary. A final RED showed remote ownership claims were not excluded from ordinary transport; both bidirectional and automatic-push argv are GREEN with `.config/workspace-claims/**` excluded.

Final review focused rerun: migration 20/20, local-rclone sync 8/8, compiled remote identity 9/9, task-schema migration 10/10, and setup 12/12 passed. Version is 0.35.1. Full release gates follow.

Final review full gates: `cargo test --release` passed with 1,306 library tests plus every integration and doc test. Strict Clippy initially exposed only mechanical warnings in the new regression tests; those warnings were corrected, the affected tests stayed green, and `cargo clippy --release --all-targets -- -D warnings` passed cleanly. The bundled-skill personal-data guard passed, and all 23 todo-skill Python tests passed. The acceptance, migration, local-rclone, watcher, literal read-only, receiver-security, OpenCode, and workspace-doc integration suites passed. Focused rustfmt over every touched Rust file, `git diff --check`, CLI help and conflicting-frontend audits, the isolated two-workspace CLI smoke, and added-line security/path/unfinished-marker/em-dash audits all passed. The temporary smoke tree was moved to Trash. No production remote, real provider credential, live TUI, or real agent PTY was used.

Final review deferred Minor: backup destination validation resolves existing canonical and lexical symlink aliases, but hardening against a symlink inserted into a previously validated missing descendant during directory creation remains deferred. Closing that filesystem race requires a separate descriptor-relative path-walk design; no unrelated backup transaction rewrite is included in this correctness pass.

Consolidated final-review wave: remote ownership RED `first_setup_attempt_stages_a_new_claim_without_publishing_canonical_identity` returned `Ok(VerifiedRemote)` and the barrier/non-atomic-copy race could not represent a late lower claim. GREEN stages every new claim without canonical or credential mutation; retry elects only from the durable claim set, and `late_competing_claims_stage_then_only_one_retry_publishes_with_non_atomic_copy` proves the lower UUID is the sole winner.

Consolidated final-review wave: migration activation RED `migration_activation_blocks_setup_before_either_can_change_remote_state` failed to compile because no activation-lock boundary existed before discovery/planning/journal creation. GREEN acquires the UUID lock before all rollout inspection and retains it through verification; setup refuses before identity while that boundary is held.

Consolidated final-review wave: remote schema RED `newer_remote_task_schema_refuses_merge_before_any_csv_publication` merged schema v3 as if it were local v2; the schema-transition integration RED could not supply remote metadata, and listed-schema read failure was indistinguishable from absence. GREEN fetches and parses remote metadata before either CSV, treats true absence as legacy only, and refuses newer, malformed, incompatible, unreadable, and legacy/current mismatches before publication. The real-rclone current-unconfigured setup test proves empty-remote schema-last transition and second-current-machine convergence.

Consolidated final-review wave: canonical header RED migration preserved source order and semantic merge preserved base order. GREEN centralizes the complete known-field order, sorts forward-compatible fields lexically, marks migrated schema as preserving them, and makes migration plus merge byte-stable.

Consolidated final-review wave: recovery RED could not express completed remote transition state. GREEN reads the durable journal step: pre-transition backup recovery retains local restore instructions, while post-transition failure is resume-only and contains no local-only `cp -pR`, `rm -f`, or restore instruction.

Consolidated final-review wave: stale-lock RED had no protected takeover seam for two barrier-synchronized contenders. GREEN prepares and syncs a PID-plus-generation owner inode before atomic hard-link publication, advisory-locks and generation-rechecks stale takeover, and yields exactly one winner. Injected owner-write failure leaves neither visible lock nor pending artifact.

Consolidated final-review wave: setup outcome RED failed to compile because baseline returned only `Result<()>`. GREEN admits credentials only after `Outcome::Clean`; `NeedsAttention`, `Aborted`, and transport errors leave them unsaved. The setup unit module passes 15/15.

Consolidated final-review wave: backup symlink RED followed a pre-existing cache-base symlink into the workspace before failing on a missing source. GREEN canonicalizes the nearest existing ancestor before overlap checks and creates nothing in the workspace. Only descriptor-relative protection against a symlink inserted after validation remains deferred.

Consolidated focused verification before final gates: strict Clippy initially found three mechanical lock/test warnings; after preserving the barrier semantics with explicit handles and simplifying the result mapping, `cargo clippy --release --all-targets -- -D warnings` passed. Setup passed 15/15, lock 6/6, recovery 2/2, header/schema transform 2/2, remote-schema focused tests 3/3, and real-rclone schema-transition tests 2/2. Version is 0.35.2. Full release gates follow.

Consolidated final gates: `cargo test --release` passed with 1,321 library tests plus every integration and doc test. The exact real-rclone remote-identity suite passed 9/9, including staged first setup and exact canonical-manifest verification. Strict Clippy, the bundled-skill personal-data guard, and all 23 todo-skill Python tests passed. Explicit acceptance 1/1, migration 21/21, local-rclone 9/9, watcher 2/2, literal read-only 15/15, receiver-security 4/4, OpenCode 7/7, and workspace-doc 27/27 suites passed. Root/workspace/migrate/sync-setup/receiver/server help, both conflicting-frontend exit-2 forms, the isolated two-workspace CLI smoke, focused rustfmt, `git diff --check`, and added-line path/unfinished-marker/stale-language/em-dash audits passed. The smoke tree was moved to Trash. No production remote, real provider credential, live TUI, or real agent PTY was used.

Final closure remote-schema RED: `cargo test --release sync::csv_merge::table::tests::present_remote_schema_requires_a_typed_complete_supported_manifest -- --exact --nocapture` failed exit 101 because the strict `remote_schema_status` boundary did not exist. The regression matrix requires true absence to mean legacy while every present malformed, missing-field, wrong-typed, incompatible, or newer manifest fails closed.

Final closure recovery RED: `every_journaled_failure_is_resume_only_even_when_remote_publication_is_ambiguous` failed because pre-backup and pre-transition journal states still emitted distinct recovery text and the completed-backup branch still advertised local restore commands. The matrix covers failure before local migration, after local migration but before remote transition, and the ambiguous remote-publication-before-journal-record window; all must retain the backup for forensic use and advertise only resume/reconcile.

Final closure nested-backup RED: `preexisting_nested_backup_symlink_into_workspace_is_rejected` returned `Ok(())` while `backup_dir/tasks` was a pre-existing symlink to the live workspace's task directory. Existing identical bytes were accepted through that alias, proving descendant components were not validated.

Final closure coordinator join RED: `cargo test --release --test sync_local coordinator_join::second_configured_legacy_machine_joins_current_remote_through_real_coordinator_and_rclone -- --exact --nocapture` failed exit 101 in the actual CLI migration coordinator after real rclone completed its generic lane cleanly. Machine A had migrated and published schema v2; configured Machine B remained legacy with a durable legacy baseline plus local edits. Its journaled `LegacySemanticSync` reached the CSV preflight and failed exactly with `remote task schema is Current, but local task schema is Legacy`, leaving the active journal before backup or cutover. The test requires task_id-keyed pre-authority reconciliation, preservation of Machine A's UUID for T1, deterministic UUID assignment for Machine B's local-only T2, successful coordinator resume/completion, and final current-state convergence across both machines and remote.

Final closure join replay RED: `cargo test --release --test multi_workspace_migration legacy_join::replayable_legacy_join_preserves_remote_uuids_and_never_publishes_legacy_shape -- --exact --nocapture` failed exit 101 because no migration-owned `join_legacy_to_current_with_transport` seam existed. The fixture independently requires two identical replays to preserve the current remote UUID for a matching task_id, merge distinct local/remote field edits, retain a blank pre-cutover UUID only for a local-only row, leave local schema legacy until the journaled cutover, and expose no remote publication callback.

Final closure strict-completeness RED: after the initial strict remote parser was GREEN, the strengthened `present_remote_schema_requires_a_typed_complete_supported_manifest` matrix still accepted a present schema-v2 object with only `task_schema_version` and `merge_key`. The exact test failed exit 101 before requiring typed `display_identity.field=task_id` and `display_identity.mutable=true`.

Final closure GREEN: the migration-owned join runs only when local state is legacy and the strictly parsed remote is current. It executes the real generic rclone lane without ordinary task reconciliation, validates both complete remote current CSVs, performs a local-only task_id three-way merge, preserves remote UUIDs for matching rows, and leaves local-only rows for deterministic cutover. The replay seam is byte-stable and the real CLI coordinator/rclone two-machine test passes through journal removal and final byte convergence. Present remote schema is authoritative and must be valid, complete, typed, and supported; only absence is legacy. Every active-journal recovery message is resume-only, including ambiguous remote publication. Pre-existing nested backup symlinks and non-directory components are rejected before copying.

Final closure focused verification: CSV sync 18/18, setup 15/15, migration 22/22, real-rclone sync 11/11, recovery 2/2, strict-schema matrix, wrong-typed compiled and real-rclone no-publication regressions, nested-backup regression, and strict Clippy are green. Version is 0.35.3. Full release gates follow.

Final closure deferred Minor: all pre-existing nested backup symlink and non-directory components are rejected. The only descriptor-relative post-validation insertion TOCTOU remains deferred Minor.

Final closure full gates: `cargo test --release` passed with 1,325 library tests plus every integration and doc test, including migration 22/22 and real-rclone sync 11/11. `cargo clippy --release --all-targets -- -D warnings` passed at version 0.35.3. The bundled-skill personal-data guard passed 1/1 and all 23 todo-skill Python tests passed. Explicit setup 15/15, stale-lock 6/6, and remote-identity 9/9 reruns passed. Focused rustfmt, `git diff --check`, exact Cargo.toml/Cargo.lock version checks, and the added-line rhetorical-em-dash audit passed. No production remote, real provider credential, live TUI, or real agent PTY was used.
